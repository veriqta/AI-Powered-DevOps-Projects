# Reusable Actions

Reusable CI components must use pinned dependencies, minimum permissions, documented inputs and outputs, tests, and a release process. Projects should pin a reviewed version instead of referencing a mutable branch.

